# =====================================================================
# .dev.vars — 로컬 개발 전용 환경 변수
# =====================================================================
# ⚠️ 절대 커밋하지 마세요 (.gitignore에 반드시 포함)
# ⚠️ 프로덕션 환경변수는 Cloudflare Pages 대시보드(Settings → Environment Variables)에 설정
# ⚙️ 사용 위치: npx wrangler pages dev 시 Pages Functions 내부의 env로 주입됨
# =====================================================================

# -----------------------------------------------------
# ① App / Runtime 설정
# -----------------------------------------------------
APP_ENV=development
APP_NAME=retro-games
APP_ORIGIN=http://127.0.0.1:8788

# -----------------------------------------------------
# ② Database (Neon / PostgreSQL)
# -----------------------------------------------------
# Neon 대시보드 → Connection Details → “psql connection string”
# ⚠️ sslmode=require 필수
NEON_DATABASE_URL=postgres://USER:PASSWORD@YOUR-NEON-HOST/neondb?sslmode=require

# -----------------------------------------------------
# ③ JWT / 인증 관련
# -----------------------------------------------------
JWT_SECRET=dev-only-secret
JWT_ISSUER=retro-games
JWT_AUDIENCE=retro-games-client
JWT_EXPIRY=7d

# -----------------------------------------------------
# ④ CORS 설정 (Cloudflare Pages dev용)
# -----------------------------------------------------
CORS_ORIGIN=http://127.0.0.1:8788
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_HEADERS=Content-Type,Authorization

# -----------------------------------------------------
# ⑤ 로깅 / 디버깅
# -----------------------------------------------------
LOG_LEVEL=debug
ENABLE_TRACE=false

# -----------------------------------------------------
# ⑥ 기타 (선택적: 향후 OAuth, Analytics, Payment 등 확장 시 사용)
# -----------------------------------------------------
# OAUTH_NAVER_CLIENT_ID=
# OAUTH_NAVER_CLIENT_SECRET=
# OAUTH_KAKAO_CLIENT_ID=
# OAUTH_KAKAO_CLIENT_SECRET=
# GA_MEASUREMENT_ID=
# SENTRY_DSN=
