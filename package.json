{
  "name": "retro-games",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --project-name=retro-db",
    "deploy": "wrangler pages deploy . --project-name=retro-db",
    "migrate": "psql \"$NEON_DATABASE_URL\" -f migrations/001_init.sql"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "hono": "^4.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.3"
  }
}
